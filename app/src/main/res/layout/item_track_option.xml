<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackground"
    android:paddingStart="16dp"
    android:paddingEnd="4dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:minHeight="48dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvPrimary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium" />

        <TextView
            android:id="@+id/tvSecondary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#999999"
            android:textSize="11sp"
            android:layout_marginTop="1dp" />

    </LinearLayout>

    <!--
        MaterialRadioButton — tinted via colorPrimary=#EF4444 from
        ThemeOverlay.MaterialComponents.MaterialAlertDialog. No buttonTint needed.
    -->
    <com.google.android.material.radiobutton.MaterialRadioButton
        android:id="@+id/radioToggle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:clickable="false"
        android:focusable="false"
        android:gravity="center" />

    <!--
        MaterialCheckBox — tinted via colorSecondary=#EF4444 from
        ThemeOverlay.MaterialComponents.MaterialAlertDialog. No buttonTint needed.
    -->
    <com.google.android.material.checkbox.MaterialCheckBox
        android:id="@+id/checkToggle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:clickable="false"
        android:focusable="false"
        android:gravity="center"
        android:visibility="gone" />

</LinearLayout>
